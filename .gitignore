# Gradle / Android build output — regenerated by every build, never commit
.gradle/
build/
app/build/
.cxx/
.externalNativeBuild/
captures/
*.apk
*.aab

# Local machine config (SDK path, your Ollama LAN address)
local.properties

# IDE
.idea/
*.iml

# OS
.DS_Store
